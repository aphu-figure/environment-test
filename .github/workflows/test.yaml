name: Test

on:
  workflow_dispatch:
    inputs:
      version:
        description: version
        required: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    environment:
      name: development
      url: https://github.com/${{ github.repository }}/releases/tag/v${{ inputs.version }}
    steps:
      - name: Test
        shell: bash
        run: |
          echo "hello world"
